name: Oracle Cloud 배포

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 코드 체크아웃
        uses: actions/checkout@v3

      - name: Node.js (24) 설치
        uses: actions/setup-node@v3
        with:
          node-version: 24

      - name: pnpm 설치
        run: npm install -g pnpm

      - name: 의존성 설치
        run: pnpm install

      - name: 빌드
        run: pnpm build

      - name: Oracle Cloud 서버 배포
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SERVER_SSH_KEY }}
          SERVER_USER: ${{ secrets.SERVER_USER }}
          SERVER_HOST: ${{ secrets.SERVER_HOST }}
        run: |
          echo "$SSH_PRIVATE_KEY" > key.pem
          chmod 600 key.pem

          echo "📦 서버에 코드 전송 중..."
          ssh -i key.pem -o StrictHostKeyChecking=no $SERVER_USER@$SERVER_HOST "mkdir -p ~/yoguhaeyo"
          rsync -avz \
            --exclude '.git' \
            --exclude 'node_modules' \
            --exclude '.github' \
            -e "ssh -i key.pem -o StrictHostKeyChecking=no" ./ $SERVER_USER@$SERVER_HOST:~/yoguhaeyo

          echo "🚀 서버에서 빌드 및 실행 중..."
          ssh -i key.pem -o StrictHostKeyChecking=no $SERVER_USER@$SERVER_HOST << 'ENDSSH'
            export NVM_DIR="$HOME/.nvm"
            [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
            export PATH="$HOME/.local/share/pnpm:$PATH"

            cd ~/yoguhaeyo
            export NODE_ENV=production

            # pnpm / pm2 없으면 설치
            if ! command -v pnpm &> /dev/null; then
              echo "🔧 pnpm 설치 중..."
              curl -fsSL https://get.pnpm.io/install.sh | sh -
              export PATH="$HOME/.local/share/pnpm:$PATH"
            fi

            if ! command -v pm2 &> /dev/null; then
              echo "🔧 pm2 설치 중..."
              npm install -g pm2
            fi

            echo "📦 의존성 설치 중..."
            pnpm install --prod

            echo "♻️ 앱 재시작 중..."
            pm2 stop yoguhaeyo || true
            pm2 start server/index.js --name yoguhaeyo --update-env
            pm2 save

            echo "✅ 배포 완료!"
          ENDSSH

          rm key.pem
